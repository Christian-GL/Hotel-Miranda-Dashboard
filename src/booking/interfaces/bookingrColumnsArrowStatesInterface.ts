
import { ArrowType } from "../../common/enums/ArrowType.ts"

export interface BookingColumnsArrowStatesInterface {
    guest: ArrowType
    orderDate: ArrowType
    checkIn: ArrowType
    checkOut: ArrowType
    roomNumber: ArrowType
}