
/* 
* Este enum se usa en lugar de un simple boolean por compatibilidad al cambiar
* la base de datos con MySQL y evitar conflictos con las conversiones.
*/

export enum OptionYesNo {
    yes = 'yes',
    no = 'no'
}